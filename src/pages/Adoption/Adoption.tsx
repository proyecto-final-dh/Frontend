type AdoptionProps = {
  pages: number;
};

const Adoption = ({ pages }: AdoptionProps) => {
  console.log({ pages });
  return (
    <div className='p-3 col-span-full'>
      {/* Junior section */}
      {/* End Junior Section */}

      {/* Felipe section */}
      {/* End Felipe Section */}
    </div>
  );
};

export default Adoption;
